# ~/.tmux.conf
# plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'mrjones2014/smart-splits.nvim'
set -g @plugin 'melnee/tmux-simple-git-status-fork'
set -sg escape-time 0
# Extended keys for slk (Shift+Enter, etc.) — supersedes xterm-keys
set -s extended-keys always

# True color support + extended keys (combined onto the same terminal name)
set-option -a terminal-features 'xterm-kitty:RGB,extkeys'
# Pass Alt+hjkl through to nvim for smart-splits resizing
bind -n M-h run-shell "~/.tmux/plugins/smart-splits.nvim/smart-splits.sh resize-left"
bind -n M-j run-shell "~/.tmux/plugins/smart-splits.nvim/smart-splits.sh resize-down"
bind -n M-k run-shell "~/.tmux/plugins/smart-splits.nvim/smart-splits.sh resize-up"
bind -n M-l run-shell "~/.tmux/plugins/smart-splits.nvim/smart-splits.sh resize-right"
# tmux git status plugin to the right side
set -g status-right-length 100
set -g status-right "#{simple_git_status}"
set -g status-interval 2
# initialize TPM (keep at bottom)
run '~/.tmux/plugins/tpm/tpm'
run '~/.tmux/plugins/tmux-simple-git-status-fork/simple_git_status.tmux'
# Colors for active window
set -g pane-border-style fg=colour238
set -g pane-active-border-style fg=colour24,bold
set -g pane-border-status top
set -g pane-border-lines heavy
# Change this to have custom text for active/inactive windows!
set -g pane-border-format " #[fg=white,bg=#7F2680,bold]#{?pane_active, ᕕ( ᐛ )ᕗ active,}#[default]#[fg=colour240]#{?pane_active,, (ಠ_ಠ) inactive } "
set -g status-style bg=colour24,fg=white
# Copy and pasting keymaps from visual mode aka ctrl+b [
# These keymaps make it so the copy and paste keyboard shortcuts are the same as they are in nvim
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "xclip -selection clipboard"
# Opens new terminal windows in the same directory as the current nvim
bind '"' split-window -v -c "#{pane_current_path}"
bind '%' split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"
# Move windows left and right to rearrange
bind S-Left swap-window -t -1\; previous-window
bind S-Right swap-window -t +1\; next-window
# Binding to open a new session
bind N new-session -c "~/Documents/coding/projects"
# Tmux pane background colors
set -s command-alias[100] red='set -p window-style "bg=#2e1616" ; set -p window-active-style "bg=#3a1a1a"'
set -s command-alias[101] blue='set -p window-style "bg=#00022c" ; set -p window-active-style "bg=#000344"'
set -s command-alias[102] green='set -p window-style "bg=#162e16" ; set -p window-active-style "bg=#1a3a1a"'
set -s command-alias[103] purple='set -p window-style "bg=#22162e" ; set -p window-active-style "bg=#2a1a3a"'
set -s command-alias[104] clear='set -p -u window-style ; set -p -u window-active-style'
set -g window-style 'bg=#121214'
set -g window-active-style 'bg=terminal'


set -g allow-passthrough on
